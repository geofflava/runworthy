from langgraph.graph import StateGraph  # agent surface -> detectors run
# FAKE credential planted for the redaction invariant test (not real):
GITHUB_TOKEN = "ghp_FAKE0123456789abcdefghijklmnopqrSTUV"
graph = StateGraph(dict)
